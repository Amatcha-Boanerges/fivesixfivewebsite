import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "../styles/globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL('https://fivesixfive.com'), // PLEASE UPDATE with your actual production URL
  title: "fivesixfive | Your Growth Partner",
  description: "Welcome to the official website of fivesixfive. We Empower Your People. We Accelerate Your Growth..",
  // TODO: Add more specific Open Graph and Twitter card metadata later (Task 1-5)
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
